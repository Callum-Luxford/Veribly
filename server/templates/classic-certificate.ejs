<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Certificate Preview</title>
    <link rel="stylesheet" href="/css/root.css" />
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
        background-color: #f8fafc;
        height: 100vh;
      }

      @page {
        size: A4 landscape;
        margin: 0;
      }
    </style>
  </head>
  <body class="flex items-center justify-center">
    <div
      id="certificate"
      class="relative flex flex-col justify-center items-center text-center px-10"
      style="
        width: 1200px;
        height: 794px;
        background-image: url('/certificate-bg/<%= template %>');
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
      "
    >
      <!-- Certificate Text Content -->
      <div class="space-y-4">
        <h1 class="text-4xl font-bold text-gray-900 uppercase">
          Certificate of Completion
        </h1>

        <p class="text-lg text-gray-700">This certificate is awarded to</p>

        <h2 class="text-3xl font-semibold text-blue-900"><%= name %></h2>

        <p class="text-base">For successfully completing</p>
        <p class="text-xl italic text-gray-800"><%= title %></p>

        <p class="text-sm mt-4">Dated: <%= date %></p>
      </div>

      <!-- Instructor Signature -->
      <div class="absolute bottom-[110px] left-[100px] text-left">
        <p class="text-xs text-gray-600">Instructor</p>
        <p class="text-md font-bold"><%= signature %></p>
      </div>

      <!-- Footer Text -->
      <div class="absolute bottom-[110px] right-[100px] text-right">
        <p class="text-xs text-gray-400 italic">Generated by Veribly</p>
      </div>
    </div>
  </body>
</html>
